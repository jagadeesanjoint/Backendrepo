# Progressive Project 1 - application.yml (database, server, logging)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mts
    username: root
    password: Abhi@2005
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true

server:
  port: 8585

logging:
  level:
    com.fidelity.mts: DEBUG
    org.springframework.security: INFO
